[package]
name         = "canton-splice-api-token-holding-v2"
version      = { workspace = true }
edition      = { workspace = true }
rust-version = { workspace = true }
license      = { workspace = true }
readme       = "README.md"
authors      = { workspace = true }
repository   = { workspace = true }
homepage     = { workspace = true }
categories   = ["api-bindings"]
keywords     = ["canton", "daml", "splice", "token-standard", "bindings"]
description  = "Pre-built typed Rust bindings for the Splice `splice-api-token-holding-v2` package (CIP-0112)."
include      = ["src/**/*", "README.md", "LICENSE"]

# Generated by `canton-codegen`. `src/lib.rs` is checked-in, generated output —
# do not edit by hand; regenerate every bindings crate together with the
# `regenerate_splice` example.
#
# The V2 token standard ships as no DAR anyone publishes: it is live on the
# network and obtained from a participant. The packages it is generated from are
# committed under `testdata/token-standard-v2`, where each file name ends with
# the package id that hashes its bytes.
[dependencies]
canton-daml = { workspace = true }
canton-daml-stdlib = { path = "../canton-daml-stdlib", version = "0.3.0" }
canton-splice-api-token-metadata-v1 = { path = "../canton-splice-api-token-metadata-v1", version = "0.3.0" }

# No `[lints]`: this crate is generated code and carries its own `#![allow(…)]`.
