//! The conformance suite lives in `tests/`; this crate has no library surface.
//!
//! It exists so `cargo test -p canton-conformance` names one thing, and so the
//! capability registry has an owner.
