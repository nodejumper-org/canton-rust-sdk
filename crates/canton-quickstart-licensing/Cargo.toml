[package]
name         = "canton-quickstart-licensing"
version      = { workspace = true }
edition      = { workspace = true }
rust-version = { workspace = true }
license      = { workspace = true }
readme       = "README.md"
authors      = { workspace = true }
repository   = { workspace = true }
homepage     = { workspace = true }
categories   = { workspace = true }
keywords     = { workspace = true }
description  = "Pre-built typed Rust bindings for the cn-quickstart `quickstart-licensing` DAR (the reference app's bindings; not published)."
publish      = false

# Generated by `dpm-codegen-rust` from quickstart-licensing-0.0.1.dar (SDK 3.5.2).
# `src/lib.rs` is checked-in generated output — do not edit by hand. Regenerate
# with the recipe in the crate README; a drift guard in `canton-codegen-cli`
# keeps it in sync with the DAR.
[dependencies]
canton-daml = { workspace = true }

# The token-API packages this DAR depends on. They are referenced rather than
# copied in, so a `Holding` read through this crate is the same Rust type as one
# read through `canton-splice-amulet` — the same Daml package, named once.
canton-splice-api-token-allocation-request-v1 = { path = "../canton-splice-api-token-allocation-request-v1", version = "0.2.0" }
canton-splice-api-token-allocation-v1 = { path = "../canton-splice-api-token-allocation-v1", version = "0.2.0" }
canton-splice-api-token-holding-v1 = { path = "../canton-splice-api-token-holding-v1", version = "0.2.0" }
canton-splice-api-token-metadata-v1 = { path = "../canton-splice-api-token-metadata-v1", version = "0.2.0" }

# No `[lints]`: generated code carries its own `#![allow(…)]`.
