[package]
name         = "canton-daml-stdlib"
version      = { workspace = true }
edition      = { workspace = true }
rust-version = { workspace = true }
license      = { workspace = true }
readme       = "README.md"
authors      = { workspace = true }
repository   = { workspace = true }
homepage     = { workspace = true }
categories   = ["api-bindings"]
keywords     = ["canton", "daml", "stdlib", "bindings"]
description  = "Pre-built typed Rust bindings for the Daml standard library (daml-stdlib, daml-prim, ghc-stdlib)."
include      = ["src/**/*", "README.md", "LICENSE"]

# Generated by `canton-codegen`. `src/lib.rs` is checked-in, generated output —
# do not edit by hand; regenerate every bindings crate together with the
# `regenerate_splice` example.
#
# The standard library ships no DAR of its own: it arrives only as somebody
# else's dependency. So this crate is generated from a *selection* of packages
# out of the DAR committed to this repository, which carries the same 29 that
# every other DAR in the corpus does.
[dependencies]
canton-daml = { workspace = true }

# No `[lints]`: this crate is generated code and carries its own `#![allow(…)]`.
